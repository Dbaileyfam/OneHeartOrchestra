/**
 * Adobe Fonts (Typekit) web project kit id — from your embed URL:
 * https://use.typekit.net/<THIS>.css
 *
 * Add “Acid Green Poster” in a web project at https://fonts.adobe.com/fonts/acid-green
 * Safe to commit: kit stylesheets are public on every site that uses them.
 *
 * You can also set `VITE_ADOBE_FONTS_KIT` in `.env` (overrides this when set).
 */
export const ADOBE_TYPEKIT_KIT_ID = "";
